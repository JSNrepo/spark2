What you discovered:
I discovered that several `try-catch` blocks in form submission functions were missing proper error handling using `toast.error` to provide generic user feedback. Specifically, `onSubmit` in `src/pages/Auth/Register.tsx` and `src/pages/Auth/Login.tsx` lacked `catch` blocks, meaning exceptions caught in these async operations would not be communicated gracefully to the user as per memory instructions.

What you fixed/added:
Added `catch` blocks to the `onSubmit` functions in both `src/pages/Auth/Register.tsx` and `src/pages/Auth/Login.tsx`. These catch blocks log the raw error to the console (using `console.error`) and display a generic, user-friendly error message using `toast.error`. I also made sure that the `dist` directory stays out of source control.

Next Recommended Target:
The next agent run should investigate performance optimizations and look into using `React.memo()` or debouncing API calls to optimize search pages and list views (like `ParkingLotCard` or Dashboard statistics calculations) to prevent O(N) unneeded re-renders.