# Run 2026-05-22 22:54

## What you discovered
- The `LocationSearch.tsx` component was missing proper screen reader attributes for decorative icons (`aria-hidden`) and interactive icon-only buttons (`aria-label`).
- The 'Clear search' icon-only button had hover states but was missing an explicit focus-visible state for keyboard users.
- The input field was missing an `aria-label`.

## What you fixed/added
- Added `aria-hidden="true"` to all decorative `lucide-react` SVGs in the component (`Loader`, `Search`, `X`, `Navigation`, `MapPin`).
- Added `aria-label` to the input based on its placeholder.
- Added `aria-label` attributes matching the `title` values to the 'Clear search' and 'Use current location' buttons.
- Added explicit keyboard focus states (`focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 rounded-md`) to the 'Clear search' button.
- Updated the `.Jules/palette.md` journal with the relevant pattern.

## Next Recommended Target
- Review other custom interactive map controls in `MapView.tsx` to ensure they have similar accessibility patterns (aria-labels and focus states).
