# Discovery
Found custom card-style radio inputs in `src/pages/Auth/Register.tsx` lacking keyboard focus indicators and grouping semantics (fieldset/legend).

# Fix
Replaced the generic div wrapper and label with `<fieldset>` and `<legend>`. Added `focus-within:ring-2` along with related focus utilities to the visual label elements to restore focus indicators.

# Next Recommended Target
Verify the `SpaceProviderRegister.tsx` to ensure its organization type selection also fully supports keyboard navigation and grouping semantics.
