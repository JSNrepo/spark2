What you discovered.
The `onProfileSubmit` function in `src/pages/Profile/ProfilePage.tsx` was using a simulated API call (`setTimeout`) instead of communicating with the Supabase backend to update user profile information.

What you fixed/added.
Updated the `onProfileSubmit` function to import the `db` utility from `../../lib/supabase` and use the `db.updateUserProfile` method with the authenticated user's ID to persist profile changes to the database. Also added error handling for the db call.

Next Recommended Target
Investigate `onPasswordSubmit` in the same `ProfilePage.tsx` file, as it also currently uses a mocked `setTimeout` instead of the actual Supabase Auth API for password changes.