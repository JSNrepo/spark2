What you discovered.
While reviewing the profile page functionality (`src/pages/Profile/ProfilePage.tsx`), I discovered that the change password form was using a mocked API call (`await new Promise(resolve => setTimeout(resolve, 1000))`) instead of actually integrating with Supabase authentication to update the user's password.

What you fixed/added.
1. Added an `updatePassword` method to the exported `auth` object in `src/lib/supabase.ts` that safely calls `supabase.auth.updateUser({ password })`.
2. Updated the `onPasswordSubmit` handler in `src/pages/Profile/ProfilePage.tsx` to call `auth.updatePassword` instead of the simulated delay, securely passing the new password and correctly handling any error responses from Supabase.

Next Recommended Target
Review other pages with placeholder logic such as `src/pages/Info/ContactUs.tsx` (which simulates form submission) or `src/pages/Booking/BookingDetails.tsx` (which simulates payment processing) to integrate them with actual backend/Supabase services.
