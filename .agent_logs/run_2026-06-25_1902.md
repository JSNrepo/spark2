# Agent Run Log

## What you discovered
I found an unhandled cleanup issue in `src/components/Map/LocationSearch.tsx`. Specifically, a timeout was created using `setTimeout` and assigned to `debounceRef.current` inside the `handleInputChange` method for search debouncing, but it was not cleared when the component unmounted.

## What you fixed/added
Added an `useEffect` hook in `src/components/Map/LocationSearch.tsx` that returns a cleanup function to explicitly clear `debounceRef.current` on component unmount, preventing memory leaks and potential state updates on an unmounted component.

## Next Recommended Target
I suggest reviewing other uses of `setTimeout` or `setInterval` across the codebase to ensure unmount cleanups are implemented properly. Another target is setting up testing frameworks like `vitest` since test coverage is currently completely missing.
