import React from 'react';import { DollarSign, Shield, Clock, Users, ArrowRight } from 'lucide-react';import { motion } from 'framer-motion';import { Link } from 'react-router-dom';import Card from '../../components/UI/Card';import Button from '../../components/UI/Button';const ListYourSpace: React.FC = () => {  const benefits = [    {      icon: DollarSign,      title: 'Earn Extra Income',      description: 'Turn your unused parking space into a steady income stream',    },    {      icon: Shield,      title: 'Secure & Protected',      description: 'All bookings are verified and protected by our platform',    },    {      icon: Clock,      title: 'Flexible Hours',      description: 'Set your own availability and pricing',    },    {      icon: Users,      title: 'Wide Customer Base',      description: 'Connect with thousands of drivers looking for parking',    },  ];  return (    <div className="min-h-screen bg-gray-50 py-12">      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">        {/* Hero Section */}        <motion.div          initial={{ opacity: 0, y: 20 }}          animate={{ opacity: 1, y: 0 }}          className="text-center mb-16"        >          <h1 className="text-4xl md:text-6xl font-bold text-gray-900 mb-6">            Start Earning with Your            <span className="text-blue-600"> Parking Space</span>          </h1>          <p className="text-xl text-gray-600 mb-8 max-w-3xl mx-auto">            Join thousands of space providers earning money from their unused parking spaces.            Simple setup, guaranteed payments, and complete control over your listings.          </p>          <Link to="/register/space-provider">            <Button size="lg" className="text-lg px-8 py-4">              Become a Space Provider              <ArrowRight className="ml-2 h-5 w-5" />            </Button>          </Link>        </motion.div>        {/* Benefits Grid */}        <motion.div          initial={{ opacity: 0, y: 20 }}          animate={{ opacity: 1, y: 0 }}          transition={{ delay: 0.2 }}          className="grid md:grid-cols-2 lg:grid-cols-4 gap-8 mb-16"        >          {benefits.map((benefit, index) => (            <motion.div              key={benefit.title}              initial={{ opacity: 0, y: 20 }}              animate={{ opacity: 1, y: 0 }}              transition={{ delay: 0.1 * index }}            >              <Card className="text-center h-full p-6">                <benefit.icon className="h-12 w-12 text-blue-600 mx-auto mb-4" />                <h3 className="text-lg font-semibold text-gray-900 mb-2">                  {benefit.title}                </h3>                <p className="text-gray-600">{benefit.description}</p>              </Card>            </motion.div>          ))}        </motion.div>        {/* How It Works */}        <motion.div          initial={{ opacity: 0, y: 20 }}          animate={{ opacity: 1, y: 0 }}          transition={{ delay: 0.4 }}          className="mb-16"        >          <h2 className="text-3xl font-bold text-center text-gray-900 mb-12">            How It Works          </h2>          <div className="grid md:grid-cols-3 gap-8">            {[              {                step: '1',                title: 'Register as Space Provider',                description: 'Create your space provider account with business details',              },              {                step: '2',                title: 'List Your Spaces',                description: 'Add your parking locations with photos and pricing',              },              {                step: '3',                title: 'Start Earning',                description: 'Receive bookings and get paid automatically',              },            ].map((step, index) => (              <motion.div                key={step.step}                initial={{ opacity: 0, y: 20 }}                animate={{ opacity: 1, y: 0 }}                transition={{ delay: 0.1 * index }}                className="text-center"              >                <div className="w-16 h-16 bg-blue-600 text-white rounded-full flex items-center justify-center text-2xl font-bold mx-auto mb-4">                  {step.step}                </div>                <h3 className="text-xl font-semibold text-gray-900 mb-2">                  {step.title}                </h3>                <p className="text-gray-600">{step.description}</p>              </motion.div>            ))}          </div>        </motion.div>        {/* Stats Section */}        <motion.div          initial={{ opacity: 0, y: 20 }}          animate={{ opacity: 1, y: 0 }}          transition={{ delay: 0.6 }}          className="bg-blue-600 rounded-2xl p-8 md:p-12 text-white mb-16"        >          <div className="grid md:grid-cols-3 gap-8 text-center">            <div>              <div className="text-4xl font-bold mb-2">$500+</div>              <div className="text-blue-100">Average monthly earnings</div>            </div>            <div>              <div className="text-4xl font-bold mb-2">24/7</div>              <div className="text-blue-100">Customer support</div>            </div>            <div>              <div className="text-4xl font-bold mb-2">98%</div>              <div className="text-blue-100">Provider satisfaction rate</div>            </div>          </div>        </motion.div>        {/* CTA Section */}        <motion.div          initial={{ opacity: 0, y: 20 }}          animate={{ opacity: 1, y: 0 }}          transition={{ delay: 0.8 }}          className="text-center"        >          <h2 className="text-3xl font-bold text-gray-900 mb-6">            Ready to Start Earning?          </h2>          <p className="text-xl text-gray-600 mb-8">            Join our community of space providers and start monetizing your parking spaces today.          </p>          <Link to="/register/space-provider">            <Button size="lg" className="text-lg px-8 py-4">              Get Started Now              <ArrowRight className="ml-2 h-5 w-5" />            </Button>          </Link>        </motion.div>      </div>    </div>  );};export default ListYourSpace;