
### UX Enhancements for Accessibility
- Added `aria-label`, `title`, and keyboard focus visible styling (`focus-visible:ring-2`) to the password visibility toggle buttons on the `Login` and `Register` pages.
- Added `aria-expanded`, `aria-label`, and keyboard focus styling to the mobile navigation toggle menu button in `Header`.

### what we are going to do next in this project
- Ensure all other icon-only interactive elements across the app have proper ARIA attributes.
- Review forms for inline error announcements and semantic label associations.
- Verify color contrast ratios for text over colored backgrounds.
